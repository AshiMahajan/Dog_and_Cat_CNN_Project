{
  "functions": {
    "api/app.py": {
      "runtime": "python3.11",
      "maxDuration": 10,
      "excludeFiles": "{dataset,reports,uploads,venv,node_modules,.git,.github}/**"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/app.py" }
  ]
}
